#pragma once

#include <iostream>

#define Print(x) std::cout << x << '\n'
#define Printerr(x) std::cerr << "[ERR] " << x << '\n'
#define Printvec(v) std::cout << "[" << v.x << ", " << v.y << "]\n"

// A huge time saver :)
